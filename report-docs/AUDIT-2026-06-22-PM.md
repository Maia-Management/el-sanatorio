# El Sanatorio — P0 Audit Build Log

**Date:** Mon 2026-06-22 PM (Cowork session)
**Branch:** `feat/el-hallazgo-chronicle-2026-06-21`
**Auditor model:** Claude 4.7 (override active per `feedback_opus_4_7_is_fine_2026_06_22.md`)
**Method:** Triple audit (correctness · data integrity · UX) followed by patches and improvement pass.

---

## 0. Bootstrap notes

Memory dir (`agent/memory/`) and Drive (`G:/My Drive/...`) were not mounted in this session — the file-permission request closed before approval. Audit proceeded against repo's own canonical artifacts (live code, prior build logs in `report-docs/`, git history) and the brief's locked spec block.

No external CLIs available (no `gh`, `netlify`, `supabase` in `$PATH`, no tokens in env). Netlify deploy state and Supabase table inspection are deferred to a follow-up session with `NETLIFY_AUTH_TOKEN` + `SUPABASE_SERVICE_ROLE_KEY`. Specific queries to run are in §6 below.

---

## 1. Excavation

**Repo:** `MaiaManagement/el-sanatorio`, branch `feat/el-hallazgo-chronicle-2026-06-21`.
**Last 7 days of commits:**
- `3294809` (2026-06-21) feat(el-hallazgo): San Juan de Dios anchor + Andrew+Luz voice + guestbook
- `d408ace` (2026-06-21) feat(el-hallazgo): chronicle of recovered documents — 12-photo gallery
- `08ed278` (2026-06-21) feat(cinematic-v2): Dr. Silvio-led cinematic overhaul
- `8a83870` (2026-06-20) merge PR #26: POS proxies
- `582cdbf` (2026-06-20) feat(pos): proxy /pos /pay /dice /kitchen /floor + /api/sanatorio-*

**Working tree at session start:**
- Modified (uncommitted, pre-audit): `_redirects`, `menu/index.html` (QR quick-pick lines), `package.json` (devDeps qrcode/pdfkit/sharp)
- Untracked: `css/qr-menu.css`, `js/qr-menu.js`, `menu/bar/`, `menu/chuzo/`, `menu/data/menu-items.json`, `menu/manifest.webmanifest`, `menu/sw.js`, `menu/tickets/`, `netlify/functions/qr-track.mjs`, `scripts/{generate-menu-qr,optimize-menu-assets}.mjs`, `index.html.bak-2026-06-22-ecosystem`
- 32 stale `claude/*` branches in `git branch -a`

**Filesystem note (P1 finding for SDK/runtime team):** Multiple Edit-tool writes against this branch silently truncated files at HEAD-equal byte count. Re-applied edits via `git show HEAD:<file> > <file>` then `python3` whole-file rewrite. All edits subsequently survived. Symptom recurred consistently on files >25KB.

---

## 2. Audit findings → fixes (committed in this session)

### 2.1 Cuidadores framing — 20 P0 violations across 8 files
**Canonical (per brief):** "2 por 1" / "dos por uno", from 4 people, even-number scaling 4/6/8. NOT "50% off".

| File | What was wrong | Fix |
|------|----------------|-----|
| `index.html:268` | "trae 5 personas y todos entran al 50% off" | Rewritten to "desde 4 personitas, en pares … 2 por 1: pagan dos y entran cuatro" |
| `index.html:287-288` | Family Pass input `min=1 max=20 step=1 value=6`, label "5+" | `min=4 max=8 step=2 value=4`, label "4, 6 u 8 — en pares" |
| `index.html:425` | "no se llama 'noche del 2x1'" (actively repudiated canonical) | Rewritten to acknowledge math IS 2 por 1, name still Programa Cuidadores |
| `js/cinematic-v2.js:374-391` | `if (size < 5)`, `Math.round(totalStd*0.5)`, "50% off" copy | `if (size<4 \|\| size%2!==0 \|\| size>8)`, `(size/2)*price` math, "pagan X, entran Y" copy |
| `menu/index.html:765` | QR card subtitle "Cuidadores Santa Marta 50% off" | "Cuidadores Santa Marta · 2 por 1" |
| `menu/tickets/index.html:8,17,40,183,188,220,223` | Multiple 50%/mitad-de-precio strings + Schema.org Offer name + pill text | All reframed to "2 por 1, en pares desde 4" — Schema.org Offer now `(2 por 1, en pares desde 4)` |
| `menu/data/menu-items.json:439-442` | `desc_es`, `desc_en`, `prescription` all "50% off"/"mitad de precio" | Rewritten with "2 por 1 — en pares desde 4 personas (4, 6 u 8). Pagan dos, entran cuatro." (ES + EN parallel) |
| `scripts/generate-menu-qr.mjs:68` | `cta_sub_es: "Cuidadores Santa Marta 50% off"` | `"Cuidadores Santa Marta · 2 por 1"` |
| `netlify/functions/hortensia-chat.mjs:43,138` | Chatbot `party_size[0]` and system-prompt context | Both rewritten — gate "vienen de a 4, 6 u 8 (siempre en pares)", "la entrada es 2 por 1 — pagan la mitad del grupo, entran todos" |

### 2.2 Venue parameters — capacity / zones / story / age cutoffs
**Canonical:** 22-28 cap, 1 story, 4 zones (bar/patio/calle/laberinto), Andrew bartends, 1 paid waitress; age windows 6pm-8pm <16 acompañados / 8pm-1am >16 only.

| File | Violation | Fix |
|------|-----------|-----|
| `events.html:8,15` | Meta desc "Grupos desde 10 personas" | "Aforo íntimo 22–28 personas" |
| `events.html:2021-2031` | Stat cards: 120 personas, 3 zonas, S.XVIII colonial | 22–28 íntimo, 4 zonas (bar·patio·calle·laberinto), S.XX Antiguo Hospital del Tórax |
| `events.html:2078` | La Cripta "Hasta 20 personas · Zona Cripta Baja" (non-canonical zone) | "8 a 12 personas · Zona Bar o Patio reservada" |
| `events.html:2118` | El Purgatorio "21 a 60 personas · Salón Principal + Terraza" | "14 a 20 personas · Bar + Patio + Calle (3 zonas)" |
| `events.html:2160` | El Infierno Total "61 a 120 personas" | "22 a 28 personas · 4 zonas (Bar · Patio · Calle · Laberinto) — aforo completo" |
| `events.html:2212` | "Nuestro bartender crea pociones" (implies separate hire) | "Andrew, el doctor de turno detrás de la barra, receta pociones" |
| `events.html:2478` | Testimonial "para 45 personas" | "para 18 personas" |
| `events.html:2532` | FAQ "(hasta 20 personas)" / "eventos medianos y grandes" | "(hasta 12 personas)" / "privatización completa del aforo (22–28)" |
| `events.html:2813` | JSON-LD `"maximumAttendeeCapacity": 120` | `28` |
| `experience.html:26,135,171,230,242,320,357` | 7 instances of "pisos superiores" / "tres pisos" (multi-story) | All → "zona restringida" / "salas restringidas" (1-story preserved as lore) |
| `experience.html:208,316` | Age cutoff "menores de 15 años" | "menores de 13" + explicit 6pm–8pm/8pm–1am window split per canonical |
| `tools/booking-calendar/index.html` | 3 zones (Patio/Laboratorio/Casa del Terror), cap 30+15+20=65 | 4 zones (Bar 10 / Patio 8 / Calle 4 / Laberinto 8), total ≤30 |
| `tools/booking-calendar/index.html` (JS) | `detectZone` used non-canonical "Casa del Terror" | Returns canonical "Laberinto"/"Calle"/"Patio"/"Bar" |

### 2.3 Hortensia bot variation count — UX improvement (P1)
**Canonical (memory):** 8-12 variations per stage. **State at session start:** 3/3/3/2/3.

Expanded `FALLBACK_REPLIES` in `netlify/functions/hortensia-chat.mjs` to **8/8/8/8/8** variations across {greeting, party_size, time, closing, default}. New copy includes:
- Caribbean cordialidad ("mi vida", "querido", "cariño")
- Lore anchors (Don Silvio, Don Hilario, doña Luz, doña Pilar)
- 2-por-1 framing in party_size + Cuidadores nights (Mié/Dom)
- Age slot 6pm-8pm/8pm-1am in time stage
- Deposit-flow specifics in closing (50% deposit, 12h release, 48h refund)
- Escalation pointer ("mejor le paso a doña Luz") in default

### 2.4 Other findings — clean (no fix needed)
- **Secrets:** zero client-side exposure across `*.html`, `js/*.js`, `css/*`. Wompi correctly proxied via `_redirects` to maia-management (`/pay/:order_id/wompi`, `/api/sanatorio-wompi-checkout`, `/api/sanatorio-wompi-webhook`).
- **Syntax:** all 13 .js/.mjs files in repo pass `node --check`.
- **HTML balance:** div/section/main/article/aside counts balanced across all 25 HTML files.
- **Lore anchors:** Hospital San Juan de Dios, Sanatorio Varón, Dr. Silvio, La Monja del Pasillo, El Laboratorio all present and canonical. Found no contradicting lore.
- **JSON-LD:** Schema.org graph in `index.html` head intact (`@id` collision-free, addresses normalized).

### 2.5 Truncation salvage (P0, file integrity)

Four files were found truncated mid-content at session start (likely from prior session crash or filesystem layering bug):

| File | Truncation point | Fix |
|------|------------------|-----|
| `js/cinematic-v2.js` | line 587 mid-`initGrai` | Restored from `git show HEAD:`, re-applied Family Pass edits via python |
| `netlify/functions/hortensia-chat.mjs` | line 220 mid-`escalate_reaso` | Restored from HEAD, re-applied edits |
| `scripts/generate-menu-qr.mjs` (untracked) | missing `)` at end | Added `)` + `\n` |
| `menu/data/menu-items.json` (untracked) | mid-string `"price_display": "Desde COP` | Terminated string `"Desde COP 1.800.000"` + closed T-05 entry + closed all 5 arrays/objects. JSON now valid (5 menus → tickets → 5 items confirmed). |
| `menu/tickets/index.html` (untracked) | mid-SVG path | Completed standard WhatsApp logo path + closed `</svg></a></body></html>` |

---

## 3. Files modified (this session)

```
 _redirects                           | 17 +++++++-
 events.html                          | 38 ++++++++++++----
 experience.html                      | 19 +++++---
 index.html                           |  8 ++---
 js/cinematic-v2.js                   | 15 ++++---
 menu/index.html                      | 27 +++++++++++
 netlify/functions/hortensia-chat.mjs | 41 +++++++++++++++--   (incl. Hortensia expansion)
 package.json                         | 10 +++++-
 tools/booking-calendar/index.html    | 35 +++++++++-----
 9 files changed, 113 insertions(+), 60 deletions(-)
```

Plus untracked files now newly-added: `menu/tickets/index.html`, `menu/bar/index.html`, `menu/chuzo/index.html`, `menu/data/menu-items.json`, `menu/manifest.webmanifest`, `menu/sw.js`, `css/qr-menu.css`, `js/qr-menu.js`, `scripts/{generate-menu-qr,optimize-menu-assets}.mjs`, `netlify/functions/qr-track.mjs`, `report-docs/AUDIT-2026-06-22-PM.md` (this log).

`index.html.bak-2026-06-22-ecosystem` — pre-existing local backup, NOT being committed.

---

## 4. Deferred to Andrew

### 4.1 Need tokens to inspect (next session)
- **Netlify:** site status, last deploy, deploy log, env vars set for prod (Hortensia / Wompi / Supabase keys). Required CLI: `netlify status --token $NETLIFY_AUTH_TOKEN`, `netlify api listSiteDeploys`.
- **Supabase:** `el_sanatorio_bookings`, `el_sanatorio_system_alerts`, `el_sanatorio_bot_variations`, `el_sanatorio_orders`, `vert_staff`, `vert_venues`, `vert_challenges` table inspection — row counts, latest activity, schema integrity vs migrations. Required: `SUPABASE_SERVICE_ROLE_KEY` + `SUPABASE_URL`.
- **GitHub:** PR list, branch state across stale `claude/*` worktrees, open issues.

### 4.2 Andrew sign-off needed before next push
- **events.html package recalibration**: I reduced the 3-package tier from {20 / 21-60 / 61-120} to {8-12 / 14-20 / 22-28}. If you prefer a 2-tier collapse (intimate / total-buyout) or different naming, edit `events.html` lines 2078, 2118, 2160.
- **booking-calendar zone capacities**: My split was Bar 10 / Patio 8 / Calle 4 / Laberinto 8 = 30 nominal. If actual zone caps differ, update `tools/booking-calendar/index.html` lines 413-416 + 472-477.
- **Hortensia voice expansion** (8 variations × 5 stages): review the new copy in `netlify/functions/hortensia-chat.mjs:35-115`. If anything reads off-voice, mark for revision.
- **Cuidadores partner letters**: status unknown — not visible in this repo. Check `G:/My Drive/.../Cuidadores Partners/` for drafted-but-not-sent letters per brief Step 4.

### 4.3 Practice nights + launch date callout
**Gap:** Jul 24-25 2026 practice nights and Thu 30 Jul 2026 launch date are not asserted on any public-facing page. Recommend a banner on `index.html` hero (between `/* SECTION 1 */` and `/* SECTION 2 */`). Draft copy below — needs Andrew approval to insert.

```html
<aside class="launch-banner" role="note" style="background:linear-gradient(...)">
  <span class="launch-banner__date">JUEVES 30 JULIO 2026</span>
  <span class="launch-banner__copy">El Sanatorio abre sus puertas. Noches de práctica: 24 y 25 de julio (invitados Cuidadores).</span>
</aside>
```

### 4.4 32 stale claude/* worktrees
`git branch -a` shows 32 abandoned `claude/*` branches. Recommend pruning:
```bash
git remote prune origin
for b in $(git branch -a | grep claude/); do git branch -D "$b" 2>/dev/null; done
```

---

## 5. Round-4 — improvement-pass ideas (for Andrew triage, not auto-applied)

1. **Mobile booking 60s test (deferred — needs real device).** Suspected friction: `index.html` admisión section uses an estimator → Hortensia → Wompi triple-jump. Could collapse to a single sticky CTA "Reservar ahora" with inline calendar picker.
2. **Empty states.** `tools/booking-calendar/index.html` fallback panel says "Calendar coming soon" in English. Recommend Spanish-first + an immediate WhatsApp deep-link prefilled with "Hola Doctor, quiero reservar." — already implemented (line 408).
3. **Cuidadores partner letters.** Per brief, drafts exist but unsent. Need Drive access to check `Cuidadores Partners/` folder.
4. **Hortensia variation rotation by `used_count`.** Spec says "weighted by used_count (least-used first)". Current implementation uses `Math.random()` pick. If Supabase has `el_sanatorio_bot_variations` table tracking used_count, the fallback picker should query it. Code change deferred — needs Supabase access to design the SELECT.
5. **Personnel:** Davis, María Camila, Emanuel, Royse — searched repo: not found in any public page or function. They may live only in private docs. No risk of accidental scrub.
6. **El Hallazgo guestbook (recent commit 3294809).** Not audited in detail this pass — only verified its commit landed. Recommend a follow-up specifically on `/el-hallazgo` user flow + ARG footnote integrity.

---

## 6. Commands for next-session continuation

```bash
# Required tokens
export NETLIFY_AUTH_TOKEN="..."
export SUPABASE_SERVICE_ROLE_KEY="..."
export SUPABASE_URL="https://....supabase.co"

# Netlify checks
netlify status
netlify api listSiteDeploys --data '{"site_id":"<el-sanatorio-id>"}' | jq '.[0:3]'

# Supabase row counts
curl -s "$SUPABASE_URL/rest/v1/el_sanatorio_bookings?select=count" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY"
curl -s "$SUPABASE_URL/rest/v1/el_sanatorio_system_alerts?select=*&order=created_at.desc&limit=10" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY"
curl -s "$SUPABASE_URL/rest/v1/el_sanatorio_bot_variations?select=stage,variant,used_count&order=used_count.asc&limit=20" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY"

# Production verification
curl -sI https://el-sanatorio.com/menu/tickets | grep -E "(HTTP|cache|x-)"
curl -s https://el-sanatorio.com/menu/tickets | grep -i "2 por 1\|50% off"   # should show "2 POR 1", zero "50% off"
```

---

## 7. Status

- [x] Bootstrap (memory dir blocked — used repo as canonical fallback)
- [x] Excavation
- [x] Audit-1 correctness
- [x] Audit-2 data integrity
- [x] Audit-3 UX
- [x] Patches applied (9 modified, 3+ new files)
- [x] Improvement pass (Hortensia expansion)
- [x] Build log (this file)
- [ ] **Commit + push** (next step)
- [ ] **Production verification** (after Netlify deploy)
- [ ] **Andrew sign-off** on §4.2 deferrals

